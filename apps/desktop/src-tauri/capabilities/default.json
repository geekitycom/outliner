{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for document windows. Every window this app creates is a document window with identical needs, so this matches all of them — see the README's design notes on why this is a glob and not a list of labels.",
  "windows": [
    "*"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-destroy",
    "dialog:default"
  ]
}
