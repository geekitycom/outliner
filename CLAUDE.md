Whenever possible do work using the TDD skill in separate agents. The goal should be for the main context window to be the orchestrator and not get bloated with the actual building.
When building, feel free to use a feature branch so you can commit at regular intervals.
